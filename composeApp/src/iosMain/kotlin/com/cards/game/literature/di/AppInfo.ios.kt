package com.cards.game.literature.di

// iOS ships from Xcode and reads its version from the bundle in native code; mirror serverUrl's
// hardcoded-iOS approach here. Android (the shipped target) reads the real values from BuildConfig.
actual val appVersionName: String = "1.1.6"
actual val appVersionCode: Int = 21
