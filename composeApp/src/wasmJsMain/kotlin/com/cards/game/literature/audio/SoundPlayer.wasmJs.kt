package com.cards.game.literature.audio

/** Spike stub — browser playback (HTMLAudio + first-gesture unlock) lands with the sound-resource move. */
actual object SoundPlayer {
    actual fun play(event: SoundEvent) {}
    actual fun release() {}
}
